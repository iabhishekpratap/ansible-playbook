---
- name: install and start service
  host: localhost
  
  tasks:
  - name: install nginx
    apt:
      name: nginx 
      state: present
  - name: starting the nginx server
    service:
      name: nginx
      state: started
      enabled: true
